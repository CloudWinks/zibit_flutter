enum DbType {
  unknown,
  blob,
  boolean,
  byte,
  byteArray,
  dateTime,
  float,
  integer,
  money,
  string,
  time,
}

enum MenuType {
  standard,
  buttons,
}
enum ControlMode {
  unknown,
  collect,
  delete,
  info,
  insert,
  list,
  update,
}

enum EnumControls {
  none,
  browser,
  dialer,
  formAdd,
  gridView,
  listView,
  mapper,
  message,
  splash,
  tabFrame,
  tableView,
}

enum EnumActions {
  none,
  add,
  edit,
  delete,
  save,
  info,
  call,
  map,
  directions,
  webSite,
  scanner,
  qrScanner,
  pickImage,
  pickContact,
}